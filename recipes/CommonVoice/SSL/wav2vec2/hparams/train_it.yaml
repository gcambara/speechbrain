 ################################
# Model: wav2vec2
# Authors: Guillermo Cambara 2021
# ################################

# Seed needs to be set at top of yaml, before objects with parameters are made
seed: 1234
__set_seed: !!python/object/apply:torch.manual_seed [!ref <seed>]
output_folder: !ref results/wav2vec2_it/<seed>
acc_file: !ref <output_folder>/acc.txt
save_folder: !ref <output_folder>/save
train_log: !ref <output_folder>/train_log.txt
use_tensorboard: True
tensorboard_logs: !ref <output_folder>/logs/

# Data files
data_folder: /disks/md1-8T/users/b.gcr/datasets/it/commonvoice/cv-corpus-7.0-2021-07-21/it  # e.g, /localscratch/cv-corpus-5.1-2020-06-22/it
train_tsv_file: !ref <data_folder>/train.tsv  # Standard CommonVoice .tsv files
dev_tsv_file: !ref <data_folder>/dev.tsv  # Standard CommonVoice .tsv files
test_tsv_file: !ref <data_folder>/test.tsv  # Standard CommonVoice .tsv files
accented_letters: False
language: it # use 'it' for Italian, 'rw' for Kinyarwanda, 'en' for english
train_csv: !ref <save_folder>/train.csv
valid_csv: !ref <save_folder>/dev.csv
test_csv: !ref <save_folder>/test.csv
skip_prep: False # Skip data preparation

# We remove utterance slonger than 10s in the train/dev/test sets as
# longer sentences certainly correspond to "open microphones".
avoid_if_longer_than: 8.0

# Training parameters
number_of_epochs: 10
lr_wav2vec: 0.0005
sorting: ascending
auto_mix_prec: False
sample_rate: 16000
ckpt_interval_minutes: 30 # save checkpoint every N min

# With data_parallel batch_size is split into N jobs
# With DDP batch_size is multiplied by N jobs
# Must be 8 per GPU to fit 32GB of VRAM
batch_size: 4
test_batch_size: 4

dataloader_options:
    batch_size: !ref <batch_size>
    num_workers: 2
test_dataloader_options:
    batch_size: !ref <test_batch_size>
    num_workers: 2

#
# Functions and classes
#
epoch_counter: !new:speechbrain.utils.epoch_loop.EpochCounter
    limit: !ref <number_of_epochs>

wav2vec2: !new:speechbrain.lobes.models.Wav2Vec2.Wav2Vec2
   #latent_extractor: None
   #vector_quantizer: None
   #context_extractor: None
   #feat_masker: None
   #loss_terms: None

modules:
   wav2vec2: !ref <wav2vec2>

model: !new:torch.nn.ModuleList
    - [!ref <wav2vec2>]

wav2vec_opt_class: !name:torch.optim.Adam
    lr: !ref <lr_wav2vec>

#lr_annealing_wav2vec: !new:speechbrain.nnet.schedulers.NewBobScheduler
#    initial_value: !ref <lr_wav2vec>
#    improvement_threshold: 0.0025
#    annealing_factor: 0.9
#    n_warmup_steps: 25000
#    patient: 0

lr_annealing_wav2vec: !new:speechbrain.nnet.schedulers.NoamScheduler
    lr_initial: !ref <lr_wav2vec>
    n_warmup_steps: 32000
    model_size: 768

checkpointer: !new:speechbrain.utils.checkpoints.Checkpointer
    checkpoints_dir: !ref <save_folder>
    recoverables:
        #wav2vec2: !ref <wav2vec2>
        model: !ref <model>
        #scheduler_model: !ref <lr_annealing_model>
        scheduler_wav2vec: !ref <lr_annealing_wav2vec>
        counter: !ref <epoch_counter>

train_logger: !new:speechbrain.utils.train_logger.FileTrainLogger
    save_file: !ref <train_log>
